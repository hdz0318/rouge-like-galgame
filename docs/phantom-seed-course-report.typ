#set document(
  title: "Phantom Seed：基于多 Agent 协同的互动叙事游戏生成系统",
  author: "022330225 黄耘青 / 202330218 何东泽 / 162340121 赵乐坤 / 162340124 吴承骏",
)
#set page(
  paper: "a4",
  margin: (left: 2.45cm, right: 2.45cm, top: 2.35cm, bottom: 2.35cm),
  numbering: "1",
)
#set text(
  font: ("Noto Serif CJK SC", "Noto Serif CJK JP", "Noto Serif CJK TC"),
  size: 11pt,
  lang: "zh",
)
#set par(first-line-indent: 2em, justify: true, leading: 0.86em, spacing: 1.18em)
#set heading(numbering: "1.1")
#show heading: it => {
  set text(font: ("Noto Serif CJK SC", "Noto Serif CJK JP", "Noto Serif CJK TC"), weight: "bold")
  block(above: 1.05em, below: 0.62em, it)
}
#show figure.caption: it => {
  set text(font: "Noto Serif CJK SC", size: 9.2pt)
  it
}
#show link: set text(fill: rgb("#1f5faa"))
#show raw: set text(font: "Noto Serif CJK SC")

#let term(body) = text(font: "Noto Serif CJK SC", weight: "semibold", body)
#let code(body) = text(font: "Noto Serif CJK SC", body)
#let cover-line(label, value) = grid(
  columns: (2.2cm, 1fr),
  gutter: 0.6cm,
  align: (right, left),
  text(weight: "semibold", label), value,
)

#align(center)[
  #v(1.2cm)
  #text(size: 12pt, tracking: 0.08em)[课程设计报告]

  #v(1.25cm)
  #text(size: 28pt, weight: "bold")[
    Phantom Seed
  ]

  #v(0.48cm)
  #line(length: 8.8cm, stroke: 0.8pt + gray)

  #v(0.62cm)
  #text(size: 18pt, weight: "bold")[
    基于多 Agent 协同的互动叙事游戏生成系统
  ]

  #v(0.45cm)
  #text(size: 11pt, fill: rgb("#555555"))[
    A Multi-Agent Interactive Narrative Game Generation System
  ]

  #v(2.25cm)
  #box(
    width: 13.6cm,
    inset: (x: 1.0cm, y: 0.2cm),
  )[
    #set par(first-line-indent: 0pt, justify: false, leading: 0.72em)
    #text(size: 11.3pt)[
      #cover-line([小组成员], [
        022330225 黄耘青 / 202330218 何东泽 \
        162340121 赵乐坤 / 162340124 吴承骏
      ])
      #v(0.36cm)
      #cover-line([课程方向], [AIGC 与智能交互系统])
      #v(0.36cm)
      #cover-line([项目仓库], [#link("https://github.com/4627488/rouge-like-galgame")[4627488/rouge-like-galgame]])
      #v(0.36cm)
      #cover-line([报告日期], [2026 年 5 月])
    ]
  ]
]

#pagebreak()

= 摘要

本课程设计以 #term[Phantom Seed] 为研究与实现对象，面向 AIGC 在互动叙事游戏中的工程化落地问题，设计并实现了一个基于多 Agent 协同的视觉小说生成系统。项目尝试将 #term[Roguelike] 的随机种子、过程式生成和多周目差异化机制，与 #term[Galgame / 视觉小说] 的角色塑造、对白推进、路线分支、情感积累和多模态演出方式结合起来，使系统能够从一个随机种子出发，生成女主角人设、角色立绘、剧情场景、分支选项、背景图像和关键剧情 CG，并在玩家交互后继续推进后续剧情。

与只生成单段文本或单张图片的 AIGC 应用不同，Phantom Seed 将生成结果视为可执行的游戏运行时数据。系统通过 Pydantic 定义 `SceneData`、`CharacterProfile`、`Choice`、`StageCommand` 等结构化协议，要求大语言模型输出可以被程序解析、校验、渲染、保存和再次回注到下一轮生成上下文中。为提高生成稳定性，项目将场景生成拆分为 Planner、Writer、Critic、Reviewer 等阶段，并结合本地质量门控、兜底场景、缓存、异步预取和存档机制，降低在线模型调用在格式、延迟和内容一致性方面的不确定性。

本文围绕系统背景、需求分析、总体架构、核心数据模型、多 Agent 生成链路、上下文工程、记忆与路线状态机、多模态图像生成、pygame 前端、异步流水线、存档恢复和质量保障等方面进行系统阐述。报告认为，本项目的主要价值在于把生成式模型从“内容输出工具”转化为“互动系统中的状态转换组件”，从而验证了 AIGC 在动态叙事、可执行内容生成和多模态游戏原型中的应用潜力。

关键词：多 Agent 系统；互动叙事；视觉小说；Roguelike；结构化输出；上下文工程；多模态生成；过程式内容生成

#pagebreak()

#outline(title: [目录], indent: auto)

#pagebreak()

= 绪论

== 研究背景

近年来，生成式人工智能在文本、图像、音频和视频生成领域取得了显著进展，大语言模型能够生成较长的自然语言文本，图像生成模型能够根据提示词生成具有较强表现力的视觉内容。这些能力为游戏内容生产提供了新的可能性。游戏研究领域长期关注过程式内容生成，相关教材与综述已经将其视为扩展游戏内容空间、降低人工创作负担和提高可重玩性的重要方法 @shaker2016pcg @togelius2011searchbasedpcg。对于叙事类游戏而言，角色设定、对白脚本、剧情分支、场景说明和美术资源都具有较高的文本与图像生产成本，因此 AIGC 很自然地成为降低原型开发门槛、提高内容变体数量的重要技术方向。

然而，互动叙事游戏并不能简单等同于文本生成任务。游戏系统要求内容具有明确结构、可执行语义和可恢复状态。例如，视觉小说场景不仅需要对白文本，还需要说话人、角色立绘、角色站位、背景、转场、玩家选项、数值变化和后续剧情钩子。如果模型输出只是一段自然语言描述，程序无法可靠地区分哪些内容应显示在对白框，哪些内容应变成可点击选项，哪些内容应改变好感度或锁定角色路线。因此，面向游戏运行时的 AIGC 系统必须解决生成内容的结构化、状态化和连续性问题。

Roguelike 游戏强调随机性、可重玩性和过程式内容生成；Galgame / 视觉小说强调角色关系、文本演出和路线分支。互动叙事研究则表明，叙事系统需要在情节结构、角色动机和玩家行动之间维持平衡，叙事规划与交互戏剧系统都围绕这一问题展开 @riedl2010narrative @mateas2002facade。两类游戏形态在 AIGC 语境下具有互补性：Roguelike 的种子机制可以为每一轮生成提供可控随机入口，视觉小说的场景结构则为模型输出提供清晰的内容容器。本项目将二者结合，提出一个以随机种子为起点、以多 Agent 生成链路为核心、以结构化场景协议为运行时边界的互动叙事生成系统。

== 课题目标

本课程设计的目标并非仅展示模型能够生成文本或图片，而是构建一个能够持续运行的 AIGC 游戏原型。具体目标包括以下几个方面。

第一，构建从随机种子到游戏内容的生成链路。系统需要根据输入种子派生角色原型、初始氛围和叙事方向，并生成多名具有不同性格、视觉锚点和关系设定的女主角。

第二，设计稳定的结构化中间协议。系统需要使模型输出能够被游戏程序解析和消费，避免自由文本直接驱动运行时所带来的不确定性。该协议需要覆盖对白、舞台调度、选项、背景、视觉类型、状态变化和连续性信息。

第三，实现多轮互动叙事闭环。玩家做出的选择应改变好感度、线路状态和历史记忆，并作为下一轮生成的输入，使后续剧情受到前序行为影响。

第四，整合文本生成、图像生成和视觉小说界面。系统需要将生成出的背景、立绘、关键情节 CG、对白和选项呈现为可操作的游戏界面，而不是停留在离线文本或调试输出。

第五，提供必要的工程保障。在线生成系统存在延迟、调用失败、格式不合规和内容波动等问题，因此需要通过异步生成、缓存、兜底场景、存档读档和质量门控提高原型可运行性。

== 报告结构

本文后续结构如下：第二章介绍相关技术与术语；第三章给出需求分析；第四章说明系统总体体系；第五章详细阐述多 Agent 生成体系；第六章介绍结构化协议与数据模型；第七章说明游戏状态、路线和记忆机制；第八章介绍图像生成与多模态闭环；第九章描述前端运行时与交互设计；第十章讨论存档、异步流水线和工程可靠性；第十一章展示实现效果；第十二章总结测试与不足；最后给出课程设计总结和参考文献。

= 相关技术与概念

== 多 Agent 协同

#term[多 Agent 系统] 指多个具有不同角色、能力或职责的智能体通过协作完成复杂任务。在传统软件工程中，复杂任务通常被拆分为需求分析、设计、实现、测试和评审等阶段；在生成式 AI 系统中，也可以采用类似思路，将单次模型调用拆解为规划、草拟、批判、修订和交付等阶段。ReAct 工作展示了语言模型在推理与行动交替中的任务求解能力 @yao2023react，Generative Agents 则进一步说明了记忆、反思和规划机制对交互式智能体行为的重要性 @park2023generativeagents。这样做的目的不是简单增加调用次数，而是把生成任务划分为更明确的子任务，使每一阶段的输入输出更清晰、质量控制更可观察。

Phantom Seed 中的多 Agent 并不是完全独立运行的多个进程，而是一个具有阶段职责划分的生成工作流。Planner 关注场景叙事目标与情绪节拍，Writer 关注对白脚本与场景正文，Critic 关注生成结果是否满足质量要求，Reviewer 关注最终结构化对象的审校与交付。这种设计适合互动叙事任务，因为叙事生成同时包含剧情目标、角色一致性、情绪节奏、用户选项和程序格式等多种约束。

== 结构化输出

#term[结构化输出] 指模型按照预先定义的结构返回数据，例如 JSON、JSON Schema 或 Pydantic 模型对应的数据格式。OpenAI 对结构化输出的介绍指出，相比只保证 JSON 合法性，按 schema 约束输出能够更好地保证字段符合预定结构 @openai2024structuredoutputs。本项目正是这种场景：游戏运行时不能依赖人工从模型文本中识别字段，而需要直接读取 `script`、`choices`、`stage_commands` 等字段。

在程序侧，项目使用 Pydantic 作为结构化数据定义与校验工具。Pydantic 文档指出，`BaseModel` 可通过字段类型定义数据模型，并在输入数据进入模型时完成解析、验证和序列化 @pydantic-models。因此，Pydantic 不只是类型注解工具，也是 AI 输出和游戏逻辑之间的安全边界。模型返回的 JSON 只有通过 Pydantic 校验，才能进入后续状态更新和 UI 渲染流程。

== 上下文工程

#term[上下文工程] 是指对模型推理时可见的信息环境进行系统设计，包括系统提示词、用户输入、工具说明、外部数据、历史消息、检索结果和长期记忆等。Generative Agents 的研究将经验记录、记忆检索、反思和计划组织为智能体架构的一部分，说明长期交互任务需要显式管理历史信息与当前行动之间的关系 @park2023generativeagents。对于互动叙事而言，上下文工程尤为重要，因为故事连续性依赖历史事件、角色关系、玩家选择和未回收伏笔。

Phantom Seed 没有把所有历史对白完整拼接到下一轮 prompt 中，而是采用“近期历史摘要 + 长期记忆碎片 + 连续性备注 + 开放伏笔”的组合方式。这种方式可以在上下文窗口有限的情况下保留高价值信息，并减少无关细节对生成质量的干扰。报告中将该策略称为渐进式上下文装载，即每个生成阶段只接收当前任务所必需的信息。

== 过程式内容生成与互动叙事

#term[过程式内容生成] 是游戏开发中常见的内容生产方式，通常根据随机种子或规则系统生成地图、关卡、敌人、道具或剧情事件。传统过程式生成多依赖明确算法和规则，相关研究已经形成包括内容表示、质量评价和搜索策略在内的系统分类 @togelius2011searchbasedpcg；AIGC 则把自然语言模型和图像模型引入生成过程，使角色设定、对白和视觉资源也能够动态生成。

#term[互动叙事] 强调玩家行为对叙事推进的影响。与线性小说不同，互动叙事需要在剧情表达和系统状态之间建立反馈关系。叙事规划研究强调情节因果与角色意图之间的平衡 @riedl2010narrative，Façade 等交互戏剧系统则提供了将自然语言交互、角色行为和戏剧管理结合起来的早期范例 @mateas2002facade。玩家选择不只是阅读路径的切换，还可能改变角色关系、情绪阶段、事件触发条件和结局分支。Phantom Seed 将好感度、线路阶段、回合数和历史记忆作为叙事状态，用这些变量影响下一轮场景生成，从而实现较基本的互动叙事闭环。

== 多模态生成

#term[多模态生成] 指文本、图像等多种信息形态的联合生成与使用。Phantom Seed 中，文本模型负责生成场景目标、对白、舞台调度和视觉描述；图像模型负责生成角色立绘、场景背景和关键情节 CG；UI 层负责把这些内容组织成视觉小说式交互界面。多模态生成的难点不只在于分别生成文本和图像，而在于两者之间需要保持语义一致性。例如，角色设定中的发型、服装和气质应当在立绘中体现；关键剧情节点中的情绪和场景应当在 CG 中得到强化。

= 需求分析

== 用户与使用场景

系统面向两类典型使用者。第一类是普通玩家，关注系统能否生成有差异、有代入感、能够持续推进的视觉小说体验。对该类用户而言，游戏应具备清晰的菜单、对白推进、选项选择、存档读档和历史回看等基础交互。第二类是课程设计评审者或开发者，关注系统是否体现 AIGC 技术路线、工程实现完整性和创新性。对该类用户而言，项目需要展示清晰的架构、可解释的模块划分、可运行的原型和可验证的输出效果。

从使用流程看，玩家进入主菜单后可以开始新游戏。系统随机产生或接收一个种子，随后生成女主角阵容、立绘、初始场景和背景图像。玩家阅读对白并在关键节点做出选择，选择会改变好感度或特定角色分数。系统根据新的状态继续生成后续剧情，逐步进入共通线、锁线窗口、个人线、关键情节阶段和结局阶段。玩家还可以进行快速存档、快速读档、打开存档菜单、查看 backlog 或调整设置。

== 功能性需求

系统的功能性需求可以概括为六类。

第一，种子驱动生成。系统应能根据随机种子生成确定性的基础参数，包括种子哈希、初始氛围和角色特征码。种子机制使每次游玩具有差异化基础，也便于调试时复现实验条件。

第二，角色生成。系统应能生成多名女主角，每名角色应包含姓名、性格、说话方式、视觉描述、标志性外观、背景故事、秘密和与玩家的初始关系。这些字段共同构成后续叙事和图像生成的角色锚点。

第三，场景生成。系统应能生成结构化场景，包括场景编号、背景、视觉类型、舞台指令、对白脚本、关键情节 CG 提示词、玩家选项、状态更新、场景目标、情绪变化、连续性备注、开放伏笔和下一幕钩子。

第四，交互反馈。玩家选择应改变状态变量，并影响后续生成。系统不仅需要显示选项，还需要把选项结果映射为好感度变化、角色路线倾向和章节推进。

第五，视觉呈现。系统应能加载背景和立绘，在对话框中逐字显示对白，在选择阶段显示可点击选项，并在关键节点切换关键情节 CG。视觉表现需符合视觉小说的基本交互习惯。

第六，存档与恢复。系统应保存完整游戏状态和生成内容，使用户能够恢复到先前进度。由于生成内容具有一次性和不确定性，存档不仅要保存数值状态，还要保存当前场景、角色数据、资源路径和对白历史。

== 非功能性需求

在线 AIGC 系统具有调用延迟、格式偏差、内容波动和外部服务失败等风险。因此，Phantom Seed 需要满足以下非功能性需求。

稳定性方面，系统应在模型调用失败或图像生成失败时继续运行。项目通过 fallback 角色、fallback 场景、默认选项、图像路径回退和异常捕获实现基本稳定性。可维护性方面，系统应采用清晰分层，使 AI 调用、状态规则、UI 渲染和存档逻辑相互解耦。可观测性方面，系统应记录多 Agent 各阶段模型、状态、token 用量和完成原因，便于分析生成质量和性能瓶颈。

响应性方面，系统应尽量减少玩家等待。由于文本和图像生成耗时较长，项目采用后台线程进行初始化和下一场景生成，并通过进度回调展示当前阶段。可扩展性方面，结构化协议应允许后续增加角色表情、动作、服装、道具、音效、语音或更复杂的路线条件。可复现性方面，种子哈希和本地缓存应帮助开发者复现特定生成结果或减少重复请求成本。

= 系统总体体系

== 总体架构概述

Phantom Seed 的系统体系可以概括为“生成服务层—结构化协议层—叙事状态层—运行时协调层—前端表现层”五层结构。该结构的核心思想是：生成式模型负责开放内容生产，但所有进入游戏运行时的数据必须经过结构化协议约束；游戏状态层负责保存确定性变量；运行时协调层负责把生成、状态和资源管理串联起来；前端表现层负责把最终结果呈现为可交互的视觉小说界面。

#figure(
  image("slidev-app/public/figures/agent-workflow.png", width: 92%),
  caption: [多 Agent 分阶段生成流程],
) <fig:agent-workflow>

生成服务层由 `OpenRouterClient` 和 `ImagenClient` 组成。前者负责调用文本模型并获取结构化输出，后者负责调用图像模型并完成本地图片处理。结构化协议层由 `protocol.py` 定义，负责描述 AI 输出的数据边界。叙事状态层由 `GameState` 和 Roguelike 规则组成，负责好感、线路、章节、历史和记忆。运行时协调层由 `GameCoordinator` 与 `AsyncPipeline` 组成，负责初始化、场景推进、资源生成、缓存和后台预取。前端表现层由 pygame-ce 相关 UI 组件构成，负责菜单、对白、选项、转场、HUD、设置和存档界面。

== 模块划分

项目源码位于 `src/phantom_seed`，主要模块如下。

`ai` 模块负责模型调用、提示词构造、结构化协议和生成链路。其内部进一步分为 `llm.py`、`imagen_client.py`、`protocol.py`、`prompts` 和 `chains`。该模块是系统中与 AIGC 能力直接相关的部分。

`core` 模块负责游戏规则和协调逻辑，包括 `GameCoordinator`、`GameState`、`SaveSystem`、`seed_engine` 和 `roguelike`。其中 `GameCoordinator` 是连接 AI 生成和游戏状态的中枢。

`pipeline` 模块负责后台生成流水线。它将耗时的初始化和下一场景生成放入工作线程中，并提供进度快照。

`ui` 模块负责 pygame-ce 前端，包括主循环、场景渲染、对白框、菜单、HUD、存档覆盖层、设置面板和转场效果。该模块不直接关注模型调用细节，而是消费已经形成的 `SceneData` 和资源路径。

`utils` 模块提供文件读写等基础工具，使 JSON 存档和配置文件处理更加集中。

== 运行流程

一次完整的新游戏流程可以分解为以下步骤。

1. 主菜单接收新游戏指令，`Engine` 创建 `GameCoordinator` 和 `AsyncPipeline`。
2. `AsyncPipeline.request_init` 启动后台初始化线程，避免阻塞 pygame 主循环。
3. `GameCoordinator.init_game` 调用 `hash_seed` 将种子转化为 SHA-256 哈希，并派生初始氛围。
4. 系统为三名女主角生成不同的种子变体，并并行调用 `CharacterChain` 生成人设。
5. 系统并行调用 `ImagenClient.generate_character_sprite` 生成角色立绘，并进行背景移除和画布归一化。
6. `GameCoordinator.get_next_scene` 组装当前状态、角色摘要、路线蓝图、历史摘要、记忆碎片和随机事件，调用 `SceneChain` 生成首个场景。
7. `SceneChain` 完成规划、正文草拟、元数据补全、质量评估和最终审校，返回 `SceneData`。
8. `GameCoordinator` 对场景进行后处理，包括选项补全、舞台指令规整、主视觉生成和转场背景预取。
9. `Engine` 进入对白阶段，逐行显示脚本；当脚本结束并存在选项时，进入选择阶段。
10. 玩家选择后，系统应用状态变化并请求下一场景生成，形成持续循环。

== 数据流与控制流关系

系统的数据流和控制流并非简单线性关系。数据流上，种子、角色设定、状态记忆和模型输出不断累积，最终形成下一次生成的上下文；控制流上，前端事件处理、后台生成线程和模型调用阶段并行协作。`GameCoordinator` 在二者之间承担协调角色：它既读取 `GameState` 生成上下文，又把 `SceneData` 的结果写回 `GameState`；既调用 AI 服务生成资源，又把资源路径交给 UI；既执行模型输出后处理，又负责 fallback。

#figure(
  image("slidev-app/public/figures/multimodal-loop.png", width: 92%),
  caption: [文本、图像、交互和状态更新构成的多模态闭环],
) <fig:multimodal-loop>

图 @fig:multimodal-loop 展示了系统的闭环特征。文本生成并不是流程终点，而是场景对象的一部分；图像生成不是静态展示，而是由场景对象中的视觉描述触发；玩家选择不是 UI 层的局部事件，而是状态机的输入；状态机更新后又影响下一轮 prompt。正因为存在这一闭环，系统才具备互动叙事意义。

= 多 Agent 生成体系

== 设计动机

互动叙事场景生成比一般短文本生成更复杂。一个合格场景需要同时满足剧情推进、角色口吻、情绪节奏、视觉表现、玩家选项和程序格式等要求。若将所有要求一次性交给单个模型调用，容易出现三个问题：一是模型可能关注文采而忽略结构字段；二是长 JSON 输出可能遗漏字段或出现格式错误；三是模型难以主动发现自身生成中的节奏问题和连续性矛盾。

因此，项目采用多阶段生成体系，把一个场景拆分为计划、草稿、元数据、评估和审校。该体系并不追求模拟真实人类团队，而是借用“分工”和“检查”的工程思想，使生成过程更接近可控流水线。每个阶段都有明确输出类型，并通过 Pydantic 模型进行校验。

== Planner：剧情规划阶段

Planner 阶段输出 `ScenePlan`。该结构包含场景目的、开场情境、情感节拍、必须使用的连续性内容、地点序列、冲突转折、回报目标、结尾钩子和选项设计。Planner 的核心价值是先建立叙事骨架，使后续正文生成围绕明确目标展开。

在视觉小说中，单个场景通常承担某种叙事功能，例如建立初印象、制造误会、推进暧昧、暴露秘密、触发锁线或收束结局。如果没有规划阶段，Writer 可能写出流畅但缺少推进价值的日常对白。Planner 通过 `scene_purpose` 和 `payoff_target` 明确本场景应完成的任务，通过 `emotional_beats` 控制情绪变化，通过 `choice_design` 为玩家交互预留分支空间。

== Writer：正文与元数据生成阶段

Writer 阶段在项目中被拆分为 `draft_script` 和 `draft_metadata`。前者生成 `SceneScriptDraft`，主要包含对白脚本、舞台指令和场景目标；后者生成 `SceneMetadataDraft`，主要包含背景、视觉类型、关键情节 CG 提示词、选项、状态更新、情绪变化、连续性备注、开放伏笔和下一幕钩子。

这种拆分具有明确工程意义。对白脚本通常较长，包含大量自然语言内容；元数据则更接近程序字段，需要保持简洁、准确和可解析。若二者混在同一长 JSON 中，模型更容易在长列表后遗漏选项或状态更新。拆分后，模型可以先专注写场景，再根据已写出的脚本补全程序所需元信息。

== Critic：质量评估阶段

Critic 阶段输出 `SceneCritique`。它从整体质量、阻塞问题、改进建议、连续性风险、选项质量和节奏质量等角度评价草稿。Critic 不直接决定最终内容，而是为是否重试和如何修订提供依据。

项目还实现了本地质量门控，使质量控制不完全依赖模型自评。本地规则包括对白条数不少于 20 条、场景切换不少于 2 次、选项数量不少于 2 个、`scene_goal` 不能为空、`next_hook` 不能为空。这些规则虽然简单，但能有效避免过短、无选项、无后续钩子的场景进入游戏。

== Reviewer：审校交付阶段

Reviewer 阶段根据计划和草稿输出最终 `SceneData`。与 Writer 相比，Reviewer 更关注整体一致性和可交付性，需要把脚本、舞台调度、选项、背景和状态更新统一成运行时可消费的对象。最终结果再次经过 Pydantic 校验，并由 `GameCoordinator` 执行后处理。

该阶段对应传统内容制作中的终稿整理。它能够在保留草稿主体内容的同时修正字段缺漏、选项不清、钩子不足或连续性表达不完整等问题。虽然这会增加模型调用成本，但对于课程设计原型而言，它显著提升了系统的结构稳定性和展示质量。

== 可观测性设计

多阶段链路如果缺少可观测性，会使调试变得困难。项目使用 `AgentStageTrace` 记录每个阶段的名称、模型、状态、摘要、prompt token、completion token、total token、结束原因和尝试次数。`SceneChain` 在每个结构化阶段结束后追加 trace。这样，开发者可以分析哪个阶段耗时或耗费 token 较多，哪个模型更容易出现结构化输出问题，以及场景生成失败发生在哪个环节。

此外，`SceneChain._format_progress` 将内部阶段转换为中文进度提示，例如“剧情规划”“正文草拟”“补全元数据”“质量评估”“剧情审校”。这些进度通过回调传递给 `GameCoordinator` 和 `AsyncPipeline`，再用于终端进度条或 UI 进度展示。这使在线生成过程对用户和开发者都更加透明。

= 结构化协议与数据模型

== 协议设计原则

Phantom Seed 的结构化协议遵循三个原则。第一，字段应对应明确的游戏运行时职责。例如 `script` 对应对白显示，`stage_commands` 对应角色站位，`choices` 对应玩家选择，`game_state_update` 对应系统状态变化。第二，协议应保留足够的叙事信息。例如 `continuity_notes` 和 `open_threads` 不直接显示在 UI 中，但会影响后续剧情连续性。第三，协议应具备容错能力。模型输出难免出现字段缺失、别名混用或局部空值，协议层应尽量通过验证器和默认值进行修复。

#figure(
  image("slidev-app/public/figures/scene-protocol.png", width: 92%),
  caption: [`SceneData` 作为模型输出与游戏运行时之间的结构化协议],
) <fig:scene-protocol>

如图 @fig:scene-protocol 所示，`SceneData` 是系统最核心的数据结构。它不是单纯的数据传输对象，而是连接生成层、状态层和 UI 层的统一接口。只要某一场景能够被表示为 `SceneData`，就可以进入现有游戏流程；反之，如果模型输出不能映射为 `SceneData`，则不能直接驱动游戏。

== `SceneData` 字段说明

`scene_id` 用于标识场景段落，便于存档、日志和 backlog 记录。`background` 表示背景描述或已生成的背景路径；在场景生成初期它通常是自然语言描述，经过图像生成后可能被替换为本地图片路径。`visual_type` 区分普通立绘场景和关键情节 CG 场景，当前包括 `SPRITE_SCENE` 与 `CINEMATIC_CG` 两类。

`stage_commands` 是舞台调度指令列表，包含角色进入、离开、移动、站位和表情。`script` 是对白行列表，每行包含说话人、对白文本、内心独白和可选的场景转场描述。关键情节 CG 提示词用于生成更具表现力的剧情画面，通常在 `visual_type` 为 `CINEMATIC_CG` 时使用。`choices` 是玩家选项列表，每个选项包含显示文本和状态变化字典。

`game_state_update` 包含是否进入关键情节阶段或结局等布尔状态。`scene_goal` 描述该场景的叙事目标，便于调试和后续评估。`emotional_shift` 描述情绪变化，帮助系统理解本场景在情感曲线中的位置。`continuity_notes` 保存必须延续的事实，`open_threads` 保存尚未回收的伏笔或问题，`next_hook` 为下一场景提供承接点。

== 角色协议

`CharacterProfile` 表示由种子生成的角色“灵魂档案”。其字段包括 `name`、`personality`、`speech_pattern`、`visual_description`、`signature_look`、`backstory`、`secrets` 和 `relationship_to_player`。这些字段分别服务于不同链路：性格和说话方式影响对白生成；视觉描述和标志性外观影响立绘生成；背景故事和秘密为长期剧情提供素材；与玩家的初始关系决定开场互动的合理性。

为了支持多女主路线，`GameCoordinator` 在初始化时生成三名女主角，并将其注册进 `GameState.heroine_affection`。后续场景生成时，系统会根据当前焦点女主、好感排行和锁线状态选择主要叙事对象。角色协议因此既是生成内容的一部分，也是路线系统的基础数据。

== 选择与状态变化

`Choice` 的核心字段是 `target_state_delta`。它使用字典表示状态变化，例如 `affection` 表示当前焦点角色好感变化，`heroine:角色名` 表示特定女主角的分数变化。这样的设计比较轻量，但足以支持多角色好感排行和锁线判断。

在后处理阶段，`GameCoordinator._normalize_choices` 会检查选项是否为空，并在必要时补充默认选项。如果处于锁线窗口，系统会确保选项能够影响特定女主角分数；如果已经锁定个人线，则会把通用好感变化映射到锁定角色。该设计避免模型生成了看似合理但不影响状态的“空选项”。

== 容错与归一化

生成式模型即使在结构化输出约束下，也可能出现局部字段缺失或字段形态不一致。项目在协议层设计了多处归一化逻辑。例如 `DialogueLine` 在 `text` 缺失时，会使用 `inner_monologue` 或 `scene_transition` 作为回退文本；`ScenePlan` 会识别 `scene_goal`、`goal`、`opening`、`setup` 等替代字段，并归一化到标准字段；地点序列和选项设计若出现字典或非字符串，也会被转换为字符串摘要。

这些处理体现了 AIGC 工程中的一个重要原则：不能假定模型永远严格、稳定、完整地服从协议。结构化协议应在“严格校验”和“合理修复”之间取得平衡。完全拒绝任何小偏差会降低可用性，完全放任偏差又会破坏运行时稳定性。本项目通过默认值、验证器和 fallback 在两者之间建立缓冲层。

= 状态机、路线与记忆机制

== 游戏状态模型

`GameState` 是系统叙事状态的集中表示。它记录总好感 `affection`、各女主好感 `heroine_affection`、当前焦点女主 `active_heroine`、线路阶段 `route_phase`、锁定线路 `route_locked_to`、回合数 `round_number`、历史摘要 `history`、长期记忆碎片 `memory_fragments`、连续性备注 `continuity_notes`、开放伏笔 `open_threads`、最近地点 `recent_locations`、上一幕钩子 `latest_hook` 和结局状态 `is_ending`。

这些变量可以分为三类。第一类是数值状态，例如好感和回合数，用于路线判断和结局分级。第二类是叙事状态，例如章节节拍、路线阶段、开放伏笔和连续性备注，用于指导后续生成。第三类是运行状态，例如是否结局、当前焦点角色和最近地点，用于前端呈现与资源生成。

== 章节节拍与路线阶段

项目定义了七个章节节拍：序章·邂逅、第一幕·接近、第二幕·心动、第三幕·波澜、第四幕·坦诚、第五幕·告白、终章·结局。这些节拍并不是固定剧本，而是为模型提供叙事阶段提示。不同回合数对应不同情感任务，使生成结果更接近视觉小说常见结构。

线路阶段包括共通线前半、共通线分歧窗口、个人线推进、个人线关键情节段落和结局幕。`update_route_state` 根据回合数、最高好感角色、领先差距和是否已锁线来更新阶段。若第 5 幕后某名女主好感达到阈值且领先明显，则进入个人线；若到第 7 幕仍未锁线，则选择当前领先角色进入个人线；第 8 幕进入关键情节阶段，第 9 幕及以后进入结局。

这种路线规则虽然简化，但具备清晰的 Galgame 结构。共通线用于塑造多名角色并提供选择空间，锁线窗口用于让玩家倾向显性化，个人线用于集中展开某一角色的矛盾与情感，关键情节阶段和结局用于完成情绪收束。

== 短期记忆与长期记忆

#figure(
  image("slidev-app/public/figures/shared-memory.png", width: 92%),
  caption: [短期上下文与长期记忆共同参与下一轮生成],
) <fig:shared-memory>

如图 @fig:shared-memory 所示，系统将记忆分为短期记忆和长期记忆。短期记忆主要由最近历史摘要、当前章节、当前焦点女主、上一选择和最新钩子组成，作用是保证下一幕能够承接上一幕。长期记忆主要由连续性备注、开放伏笔和记忆碎片组成，作用是避免角色设定、事件因果和未回收信息在多轮生成中丢失。

`remember_scene` 在每次场景生成后提取并保存 `continuity_notes`、`open_threads`、最近地点和 `next_hook`。`add_history` 记录每场景摘要，并限制历史长度为最近 15 条，防止上下文无限增长。每三幕系统还会根据历史生成一个记忆碎片，并保留最近 8 条。虽然当前记忆碎片仍是简单文本摘要，但已经体现了“压缩历史、保留关键事实”的思想。

== 渐进式上下文装载

#figure(
  image("slidev-app/public/figures/progressive-disclosure.png", width: 92%),
  caption: [渐进式上下文装载：只向当前 Agent 提供必要信息],
) <fig:progressive-disclosure>

系统没有把完整对白历史直接传入模型，而是通过 `get_history_summary` 和 `get_story_memory` 组织上下文。前者返回最近场景摘要，后者返回关系阶段、章节节拍、路线阶段、焦点女主、锁线状态、好感排行、必须延续的事实、未回收伏笔、最近地点和长期记忆碎片。这样，模型看到的是经过筛选的叙事状态，而不是冗长的原始日志。

该机制对生成质量有两方面意义。首先，它减少了无关上下文对模型注意力的干扰，使模型更容易围绕当前叙事目标展开。其次，它使不同 Agent 可以基于相同状态摘要执行不同任务。例如 Planner 更关注章节、路线和伏笔，Writer 更关注角色口吻和当前场景，Critic 更关注连续性风险和选项质量。

== 随机事件与 Roguelike 元素

`roguelike.py` 定义了随机恋爱事件池，例如突然下雨、自动贩卖机偶遇、一起值日、图书馆碰手、学园祭准备等。`roll_random_event` 根据基础概率、好感加成和回合加成决定是否注入随机事件。随着回合推进和好感提高，随机事件出现概率增加。

这些随机事件承担轻量级 Roguelike 功能：它们为每一轮生成提供不同的触发点，使相同路线阶段也可能展开不同场景。与传统 Roguelike 中的战斗事件或地图变化不同，本项目的随机性服务于情感叙事，即通过日常事件制造角色互动契机。

= 多模态生成与资源管理

== 图像生成链路

Phantom Seed 的图像生成由 `ImagenClient` 负责。它统一封装角色立绘、背景和关键情节 CG 的生成请求，并通过 OpenRouter 图像模型返回的 base64 数据生成本地 PNG 文件。系统使用不同模板控制不同类型图像：角色立绘强调单一主体、透明背景和视觉小说风格；背景强调横向场景、空间氛围和可作为舞台的画面；关键情节 CG 强调关键情绪、构图张力和展示效果。

图像生成并不是独立于文本的流程。角色立绘使用 `CharacterProfile.visual_description` 作为主要提示；场景背景使用 `SceneData.background`；关键情节 CG 使用对应的剧情画面提示词，并可传入角色立绘和当前背景作为参考。这样，文本生成结果直接决定图像生成方向，图像结果又进入 UI 呈现和存档系统。

== 角色立绘处理

视觉小说界面对角色立绘有特殊要求：主体需要居中，背景需要透明，尺寸需要稳定，底部位置需要统一。生成模型直接输出的图片往往带有白色或复杂背景，主体大小和位置也不稳定。因此项目实现了立绘后处理流程。

首先，系统尝试使用 `rembg` 进行背景移除。如果该依赖不可用或处理失败，则回退到基于角点颜色采样的近白背景去除。其次，系统通过 alpha 通道寻找最大不透明连通区域，提取主要人物主体，过滤多余碎片。最后，系统将主体缩放到固定 1024×1536 画布中，并以脚底边距对齐。经过处理后，立绘更适合在不同场景中复用，也更容易实现多角色同屏调度。

== 背景缓存与并发控制

图像生成成本较高，重复请求会增加等待时间和费用。项目使用 `_cache_path` 根据模型、完整提示词和参考图路径生成 SHA-256 文件名；若缓存文件存在，则直接复用。对于背景，`GameCoordinator` 还维护 `_bg_cache`，将背景描述归一化为短键，映射到本地图片路径。

为了避免多个线程同时生成同一背景，系统使用 `_bg_lock` 和 `_bg_inflight` 记录正在生成的背景。如果某个背景已经处于生成中，其他线程会等待对应事件，而不是发起重复请求。该设计体现了资源管理中的并发控制思想。

== 转场背景预取

`SceneData.script` 中的每一行对白可以包含 `scene_transition` 字段，表示在该行之前切换背景。`GameCoordinator._generate_transition_bgs_async` 会收集这些转场描述，并以后台线程方式预生成转场背景。由于转场背景不一定在场景开头立即显示，系统可以利用玩家阅读对白的时间进行预取，从而降低后续等待。

该机制体现了“阅读时间掩盖生成时间”的设计思路。视觉小说通常有较长对白阅读过程，系统可以在玩家阅读期间准备即将使用的资源。这与 `AsyncPipeline` 预取下一场景的思想一致，都是为了改善在线生成带来的延迟体验。

= 前端运行时与交互系统

== pygame-ce 主循环

前端运行时由 `ui/engine.py` 实现。pygame 官方文档指出，pygame 应用通常通过显示窗口、Surface 更新和事件队列处理构建游戏循环 @pygame-display。本项目的 `Engine.run` 遵循这一模式：初始化 pygame 和 UI 组件，在循环中按帧处理事件、更新状态并渲染画面，最后在退出时调用 `pygame.quit`。

系统定义了 `GamePhase` 枚举，包括 `MAIN_MENU`、`LOADING`、`DIALOGUE`、`CHOICE`、`TRANSITION` 和 `ENDING`。这些阶段构成前端状态机。不同阶段接收不同事件：主菜单阶段处理新游戏、继续、读档、设置和退出；对白阶段处理鼠标点击、空格推进、自动模式和跳过模式；选择阶段处理选项点击；覆盖层打开时，存档或设置界面会拦截事件。

== 视觉小说交互

视觉小说交互的核心是“阅读—推进—选择”。`DialogueBox` 负责显示说话人、对白文本和内心独白，并支持逐字显示与跳过。玩家可以通过鼠标左键或空格推进对白；在文本尚未显示完时，操作会先显示完整文本；文本显示完后，操作进入下一行。该交互符合多数视觉小说玩家的预期。

`ChoiceMenu` 负责显示选项。场景脚本结束后，如果当前 `SceneData` 包含选项，系统进入 `CHOICE` 阶段。玩家选择某一项后，系统读取其 `target_state_delta`，交给 `GameCoordinator.get_next_scene` 应用状态并生成下一场景。这样，UI 行为和叙事状态之间形成明确映射。

== 菜单、HUD 与覆盖层

系统实现了主菜单、HUD、存档菜单、读档菜单、backlog 和设置面板。主菜单提供新游戏、继续、读档、设置和退出等入口。HUD 用于展示当前状态和常用控制。存档覆盖层既可用于保存和读取，也可用于显示历史对白。设置面板支持文本速度、自动播放间隔和显示模式等配置。

这些功能虽然不是 AIGC 算法本身，但对于完整游戏原型非常重要。没有菜单、存档和设置，系统更像技术演示；加入这些交互后，项目更接近可体验的视觉小说应用。

= 异步流水线、存档与工程可靠性

== 异步生成流水线

在线生成文本和图像通常需要数秒到数十秒。若所有生成都在主线程中执行，pygame 窗口会卡顿甚至无响应。项目因此实现了 `AsyncPipeline`，使用后台线程处理初始化和下一场景生成。

`AsyncPipeline` 维护 `_prefetched`、`_error`、`_generating`、`_worker` 和 `_request_id`。`request_init` 用于后台初始化新游戏，`request_next` 用于后台生成下一场景。`_request_id` 用于避免旧请求结果覆盖新请求结果；`get_progress_snapshot` 用于向 UI 提供当前阶段、进度、消息和耗时。该实现与 pygame 主循环兼容，避免了在图形事件循环中引入复杂 asyncio 调度。

== 存档系统

`SaveSystem` 将每个存档槽保存为 `.saves/slot_*.json`。`SaveData` 包含版本、槽位、时间戳、种子哈希、初始氛围、好感、女主好感、当前焦点女主、线路阶段、锁线对象、回合数、历史、记忆碎片、连续性备注、开放伏笔、最近地点、最新钩子、是否结局、角色数据、立绘路径、背景缓存、当前场景、对白索引、backlog 和缩略图。

这种保存粒度比传统数值存档更高，原因在于生成式游戏中的文本和图像本身也是运行时状态的一部分。如果只保存好感和回合数，再次读取时重新调用模型，可能得到完全不同的剧情和图像，从而破坏玩家体验。因此，系统必须保存当前生成内容及其资源路径。

== 兜底机制

项目在多个层面设置了兜底机制。角色生成失败时，`GameCoordinator._fallback_heroine` 提供预设角色模板。场景生成失败时，系统使用 `FALLBACK_SCENE`，保证游戏仍能进入可显示场景。选项为空时，`_normalize_choices` 自动补充“靠近某角色”和“暂时保持距离”等默认选项。图像生成失败时，系统保留原有描述或路径，不使整个流程崩溃。

这些兜底机制对于课程设计原型非常关键。AIGC 系统依赖外部服务，任何网络错误、模型错误或格式错误都可能导致中断。兜底机制使项目具备更好的运行稳定性，也体现了工程实现对不确定性的处理能力。

== 质量门控与测试

项目包含 pytest 测试文件，覆盖工具 IO、保存数据、异步流水线和场景链路等模块。除测试外，运行时还包含质量门控。`SceneChain._passes_local_quality_gate` 检查对白数量、转场数量、选项数量、场景目标和下一钩子。Critic 阶段提供模型侧质量判断，本地门控提供确定性规则判断，两者共同决定是否进行下一轮草稿生成。

质量门控的意义在于防止明显不完整的场景进入 UI。例如，如果场景没有选项，玩家就无法产生交互；如果没有 `next_hook`，下一幕承接可能变弱；如果对白过短，视觉小说体验会显得空洞。虽然这些规则不能全面衡量文学质量，但能保障基础可玩性。

= 实现效果与结果分析

== 运行效果展示

项目已经能够展示单角色、多角色、分支选择和关键情节 CG 等典型场景。图 @fig:screens 展示了系统当前的主要运行效果。

#figure(
  grid(
    columns: (1fr, 1fr),
    gutter: 0.35cm,
    image("slidev-app/public/screenshots/new/single.png", width: 100%),
    image("slidev-app/public/screenshots/new/mulit.png", width: 100%),

    image("slidev-app/public/screenshots/new/choice.png", width: 100%),
    image("slidev-app/public/screenshots/new/key-scene.png", width: 100%),
  ),
  caption: [运行效果：单角色、多角色、分支选择和关键情节 CG],
) <fig:screens>

单角色剧情场景展示了背景、立绘和对白框的联动，说明生成内容已经被整合进标准视觉小说界面。多角色场景展示了舞台调度能力，说明系统可以根据 `stage_commands` 让多名角色进入同一画面并安排左右中站位。分支选择界面展示了玩家可操作选项，说明模型输出的 `choices` 已经映射到 UI 控件。关键情节 CG 展示了系统对重要情绪节点的特殊视觉处理，说明 `visual_type` 能够驱动不同图像生成路径。

== 系统特点分析

从结果看，Phantom Seed 具有三个主要特点。第一，生成内容具有可执行性。文本、图像和选项不是独立素材，而是通过 `SceneData` 进入游戏循环。第二，叙事推进具有状态依赖。每轮生成都读取好感、线路、历史和记忆，因此后续剧情会受到玩家选择影响。第三，系统具有较完整的运行时支撑。主菜单、存档、读档、backlog、设置、进度提示和缓存机制使原型具备基本可体验性。

与静态 AIGC 展示相比，本项目更强调“系统性”。单次生成结果的美观程度固然重要，但课程设计的核心价值在于将模型能力封装为可维护的游戏系统。结构化协议、多 Agent 流水线、状态机和 UI 前端共同构成了完整体系。

== 与课程目标的对应关系

从 AIGC 课程角度看，本项目覆盖了文本生成、图像生成、结构化输出、提示词设计、多 Agent 协同、上下文管理和工程部署等多个知识点。文本生成用于角色和场景；图像生成用于立绘、背景和 CG；结构化输出用于运行时协议；提示词设计用于控制不同 Agent 阶段；上下文管理用于多轮叙事连续性；工程部署则体现在 pygame 前端、存档系统和异步流水线中。

因此，项目不是单一算法实验，而是一个包含生成模型应用、软件架构设计和交互系统实现的综合课程设计。

= 不足与改进方向

== 当前不足

首先，图像风格一致性仍然有限。即使角色视觉描述中包含发型、服装和气质锚点，多次生成仍可能出现细节漂移。尤其在关键情节 CG 中，角色脸部、服饰和立绘之间可能存在差异。

其次，长期记忆机制仍较简单。当前系统主要使用文本摘要、连续性备注和开放伏笔列表，尚未引入向量检索、事件图谱或更精细的事实数据库。因此，当剧情长度进一步增加时，角色关系和伏笔管理可能仍会出现遗漏。

再次，多阶段生成带来较高延迟和成本。Planner、Writer、Critic、Reviewer 的完整链路提高了质量和稳定性，但每个场景需要多次模型调用。对于实际游戏产品，可能需要根据场景重要性动态选择生成深度。

最后，自动评测体系仍不完善。当前测试主要覆盖程序逻辑和部分链路，尚缺少针对剧情质量、角色口吻一致性、选项有效性、图像文本一致性和玩家体验的系统评估指标。

== 后续改进

后续可以从四个方向改进。第一，引入更强的角色一致性约束，例如固定角色参考图、表情差分、服装模板和角色视觉词表。第二，完善记忆管理，引入结构化事件记录、角色关系图谱和基于检索的长期记忆。第三，优化生成策略，根据场景类型选择不同链路：普通日常场景可使用较轻量模型，锁线、关键情节和结局场景使用完整多 Agent 审校。第四，建立自动评测体系，从结构完整性、连续性、选项质量、情绪节奏和资源生成成功率等维度统计系统表现。

此外，系统还可以扩展语音合成、背景音乐、角色表情变化、动画转场、道具系统和更复杂的数值机制，使其从原型逐步接近完整互动叙事产品。

= 课程设计总结

Phantom Seed 围绕“生成结果能否进入可运行互动系统”这一核心问题展开。项目将 Roguelike 的种子机制与视觉小说的情感叙事结构结合起来，以多 Agent 生成链路生产场景，以 `SceneData` 结构化协议连接模型输出和游戏运行时，以 `GameState` 管理好感、线路、历史和记忆，以 pygame-ce 构建可交互前端，以异步流水线、缓存、存档和兜底机制提高系统稳定性。

本项目的主要贡献可以概括为三点。第一，建立了一个面向互动叙事的结构化 AIGC 生成框架，使模型输出不再是孤立文本，而是可解析、可渲染、可保存和可继续推进的游戏数据。第二，将多 Agent 协同具体落地到剧情规划、正文生成、质量评估和最终审校的阶段化流程中，提高了复杂场景生成的可控性。第三，实现了文本、图像、交互和状态更新的多模态闭环，验证了 AIGC 技术在动态叙事游戏原型中的可行性。

从课程设计角度看，该项目综合运用了生成式 AI、数据建模、游戏状态机、前端交互、异步执行和工程容错等知识。其不足主要集中在生成一致性、长期记忆和自动评测方面，但这些不足也为后续研究提供了明确方向。总体而言，Phantom Seed 表明，AIGC 在游戏中的价值不应局限于静态素材生产，而应进一步进入系统流程，成为可驱动互动体验的运行时组成部分。

= 参考文献

#set par(first-line-indent: 0pt)

#cite(<phantom-seed-repository>, form: none)

#bibliography("phantom-seed-references.bib", title: none, style: "gb-7714-2015-numeric")
